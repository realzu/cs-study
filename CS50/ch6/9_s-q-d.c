// 6-9 스택, 큐, 딕셔너리

/*큐 queues
FIFO, 선입 선출 (식당 줄서기 - 내가먼저줄서서 내가먼저받음 -> 그 뒤에선사람도 차례대로 받음)

enqueue : 줄에 들어가서 서는 것 (
dequeue : 줄을 빠져나오는 것 (ex.음식완료)
*/

/*스택
LIFO, 후입 선출
쟁반 가져가기 (가장 위의 것부터 가져가기), 이메일 수신함(최신메일이 맨 위)

push : 스택에 어떤 요소를 밀어 넣는다
pop : 가장 위의 요소를 뺀다
*/

/* 딕셔너리
키-값 : 키에 해당하는 값을 저장하고 읽어옴 (ex. 학번-학생)
쌍으로 이루어진 자료
*/
